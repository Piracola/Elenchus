1. 用户界面是一个多agent群聊系统，可以上下滑动，辩手消息宽度大于对应的裁判消息，宽度按6:4的比例确定消息块大小。
2. 每个agent和裁判都可以完整读取到所有消息内容。
3. 每个agent或用户的消息块都和对应的裁判评分消息块位置绑定，顶部对齐，避免消息长短不一导致块尺寸不同带来的消息块对应错误。在渲染每个辩手消息和裁判消息下一条消息时检测底部，以最大高度来确定下一条消息起始位置，避免左右消息错位。
4. 对应的agent头像使用红绿色块来表示正反方
5. 头像旁标注agent信息，如正反方，一辩或是二辩，模型信息